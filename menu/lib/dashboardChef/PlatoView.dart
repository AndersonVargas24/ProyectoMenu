import 'package:flutter/material.dart';

class viewchef extends StatefulWidget {
  const viewchef({super.key});

  @override
  State<viewchef> createState() => _viewchefState();
}

class _viewchefState extends State<viewchef> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      fallbackHeight: 600,
      fallbackWidth: 400,
      color: Colors.red,
      strokeWidth: 2,
    );
  }
}